% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/post_completion_terms.R
\name{post_completion_terms}
\alias{post_completion_terms}
\title{Identify terms after the first completion term}
\usage{
post_completion_terms(dframe, midf_table = degree)
}
\arguments{
\item{dframe}{Data frame or data frame extension (e.g., data.table or tibble) with required variables \code{{mcid}} and one of
\verb{\{term, term_course, term_degree\}.}}

\item{midf_table}{\code{degree} data frame with required variables
\verb{\{mcid, term_degree\}.}}
}
\value{
Data frame with the following properties:
\itemize{
\item Data frame class is preserved. Groups and keys are
not preserved.
\item Row order is preserved. Rows with \code{NA} values in any
of the required variables are removed. Duplicated rows are removed.
\item New columns are added or replace existing columns of the same name (if
any). Other columns are not modified. The following variables are added:
\itemize{
\item \code{first_term_degree}    Character. Term of a student's first
baccalaureate, encoded \code{YYYYT} or, if no degree recorded, \code{NA}.
Joined from the \code{term_degree} variable in \code{midf_table.}
\item \code{term_category.}    Character, indicating that a term belongs
to one of three clusters: terms that are prior to ("pre-degree"),
equal to ("first-degree"), or subsequent to ("post-first-degree")
the student’s first degree term.
}
\item Groups and keys are not preserved.
}
}
\description{
For each student's term in a data frame, determine its relationship to the
student's first degree term (pre-degree, first-degree, or post-first-degree)
and add columns that support the findings. Post-first-baccalaureate terms
are typically excluded from the \verb{term, course,} and \code{degree} data tables.
}
\details{
In a typical analysis, one is interested in a student's progress up to
and including the term in which they earn their first degree or degrees.
Any terms later than the first baccalaureate can usually be excluded from
study.
}
\examples{
# select min required columns
term <- toy_term[, .(mcid, term)]
course <- toy_course[, .(mcid, term_course)]
degree <- toy_degree[, .(mcid, term_degree)]

# labeling pre- and post-completion terms
term <- post_completion_terms(term, midf_table = degree)
course <- post_completion_terms(course, midf_table = degree)
degree <- post_completion_terms(degree, midf_table = degree)

# results
term[order(-before_or_after)]
term[, .N, by = "before_or_after"][order(-N)]

course[order(-before_or_after)]
course[, .N, by = "before_or_after"][order(-N)]

degree[order(-before_or_after)]
degree[, .N, by = "before_or_after"][order(-N)]
}
