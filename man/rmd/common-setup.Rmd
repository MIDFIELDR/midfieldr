```{r child-common-setup}
#| include: false

# code chunks
knitr::opts_chunk$set(fig.width = 8,
                      out.width = "100%",
                      collapse  = TRUE, 
                      comment   = "#>",
                      message   = FALSE, 
                      cache     = FALSE, 
                      error     = FALSE,
                      tidy      = FALSE, 
                      echo      = TRUE)

# inline numbers with comma separators
knitr::knit_hooks$set(inline = function(x) {
    if (!is.numeric(x)) {
        x
    } else if (x >= 10000) {
        prettyNum(round(x, 2), big.mark = ",")
    } else {
        prettyNum(round(x, 2))
    }
})

# backup existing random number seed
oldseed <- NULL
if (exists(".Random.seed")) oldseed <- .Random.seed

# if Nrows equal to or less than this N, print all rows
options(datatable.print.nrows = 6) 

# if Nrows > N above, print this N rows
options(datatable.print.topn = 3)

# class of each column
options(datatable.print.class = TRUE)

# backup options, reset on closing
my_options <- options()



# ---------- consistent function description

completion_status_one_line <- 
    "Identifies students completing a program in a timely manner."
data_sufficiency_one_line <- 
    "Identifies records to exclude due to insufficient data."
filter_programs_one_line <- 
    "Helps in finding 6-digit program codes."
initialize_fye_proxies_one_line <- 
    "Conditions data for imputing starting majors of First-Year Engineering (FYE) students."
order_multiway_one_line <- 
    "Conditions multiway data for Cleveland multiway charts."
timely_term_one_line <- 
    "Determines the latest term by which program completion would be considered timely."
undergrad_term_id_one_line <- 
    "Distinguishes between undergraduate and post-baccalaureate terms."
```
