*Note on syntax.* &nbsp; In our examples, we use `data.table` for data manipulation and `ggplot2` for charts. Some users may prefer base R, `dplyr` (for data), or `lattice` (for charts).  Each system has its strengths---users are welcome to translate our examples to their preferred syntax.
